<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QUICKSALES.Views.LoginPage"
             BackgroundColor="#F8D7DA" 
             NavigationPage.HasNavigationBar="False">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="CustomEntryStyle" TargetType="Entry">
                <Setter Property="TextColor" Value="Black"/>
                <Setter Property="PlaceholderColor" Value="Gray"/>
                <Setter Property="FontSize" Value="Medium"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
  

    <ContentPage.Content>
        <StackLayout Padding="20" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
            <Frame BackgroundColor="White" Padding="60" CornerRadius="8" HasShadow="True">
                <StackLayout>
                    <Image Source="https://i.ibb.co/HtGdZ3C/Logo-png.png" />
                    <Entry x:Name="UsernameEntry" Placeholder="Usuario" Margin="0,10" TextColor="Black" PlaceholderColor="Gray" BackgroundColor="LightGray" HeightRequest="50" FontSize="Medium" />
                    <Entry x:Name="PasswordEntry" Placeholder="Contraseña" IsPassword="True" Margin="0,10" TextColor="Black" PlaceholderColor="Gray" BackgroundColor="LightGray" HeightRequest="50" FontSize="Medium" />
                    <Button Text="Iniciar Sesión" Clicked="OnLoginButtonClicked" BackgroundColor="#000000" TextColor="White" Margin="0,10" CornerRadius="5"/>
                    <Button Text="Registrar Usuario" Clicked="OnRegisterButtonClicked" BackgroundColor="#000000" TextColor="White" Margin="0,10" CornerRadius="5"/>
                </StackLayout>
            </Frame>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
